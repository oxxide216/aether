(use 'std/core.ae')

(let fmt [args] ->
  (if (== (len args) 0)
    ''
   elif (== (len args) 1)
    (head args)
   else
    (+ (head args) (<-> (tail args)))))

(let title 'Hello world')
(println (fmt ['<title>' title '</title>']))
(println (fmt []))
