(use '../std/lib.ae')

(let a (str-to-num (input)))
(let b (str-to-num (input)))

(if (== a b)
  (println 'equal'))

(if (!= a b)
  (println 'not equal'))

(if (< a b)
  (println 'less'))

(if (<= a b)
  (println 'less or equal'))

(if (> a b)
  (println 'greater'))

(if (>= a b)
  (println 'greater or equal'))

(println (bool-to-str (! true)))

