(fun print-list [list]
  (fun print-list-rec [list]
    (if (! (is-empty list))
      (print (head list))
      (if (! (is-empty (tail list)))
        (print ' '))
      (print-list-rec (tail list))))
  (print '[')
  (print-list-rec list)
  (println ']'))

(let list [0 1 2])
(+ -1 list)
(+ list -2)

(println (nth list 2))
(println (nth list 10))
(println (len list))
(println (get-range list 1 4))
(print-list list)
(println list)
