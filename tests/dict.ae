(use 'std/core.ae')

(let user {
  'name': 'John'
  'surname': 'Doe'
  'age': 18
})

(println user)
(println (get-in user 'name'))
(set-in user 'name': 'Paul')
(set-in user 'age': (+ 1 (get-in user 'age')))
(println (get-in user 'name'))
(println user)
