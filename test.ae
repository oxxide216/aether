(use 'std/core')
(use 'std/base')

(macro test \testt ->
  (+ testt 2))

(printf [(test 4) '\n'])

(macro variadic \...args ->
  (printf [args '\n']))

(variadic 0 1 2)

(if 1
  (printf ['hello world\n'])
 else
  (printf ['foo, bar\n']))

(let text 'hi!!!!')
(\-> (printf [text '\n']))

(let i 0)
(while (< i 10)
  (printf [i '\n'])
  (set i (+ i 1)))
