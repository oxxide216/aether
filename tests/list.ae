(let print-list [list] ->
  (let print-list-rec [list] ->
    (if list
      (print (head list))
      (if (tail list)
        (print ' '))
      (<-> (tail list))))
  (print '[')
  (print-list-rec list)
  (println ']'))

(let list [0 1 2])
(set list (+ -1 list))
(set list (+ list -2))

(println list::2)
(println list::10)
(println (len list))
(println (get-range list 1 4))
(print-list list)
(println list)
