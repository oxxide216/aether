(use '../std/base.ae')
(use '../std/system.ae')

(if (< (len (get-args)) 3)
  (println 'ERROR: input file was not provided')
 else
  (println (len (split (read-file (head (tail (tail (get-args))))) '\n'))))
