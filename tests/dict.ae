(let user {
  'name': 'John'
  'surname': 'Doe'
  'age': 18
})

(println user)
(println (get-at user 'name'))
(set-in user 'name': 'Paul')
(set-in user 'age': (+ 1 (get-at user 'age')))
(println (get-at user 'name'))
(println user)
