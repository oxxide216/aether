(use 'std/core.ae')

(let user {
  'name': 'John'
  'surname': 'Doe'
  'age': 18
})

(println user)
(println (get user 'name'))
(set-in user 'name': 'Paul')
(set-in user 'age': (+ 1 (get user 'age')))
(println (get user 'name'))
(println user)
